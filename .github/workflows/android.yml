# LotteryLab Mobile Android Build

name: Build LotteryLab Mobile APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-android:
    name: Build Android APK
    runs-on: ubuntu-24.04

    steps:

      # ---------------------------------------------------------
      # CHECKOUT
      # ---------------------------------------------------------

      - name: Checkout repository
        uses: actions/checkout@v4

      # ---------------------------------------------------------
      # PYTHON 3.14.2
      # ---------------------------------------------------------

      - name: Set up Python 3.14.2
        uses: actions/setup-python@v5
        with:
          python-version: "3.14.2"

      - name: Verify Python 3.14.2
        run: |
          set -eux

          echo "========================================"
          echo "PYTHON VERSION"
          echo "========================================"

          python --version
          python -m pip --version

          python -c "import sys; print(sys.executable)"
          python -c "import sys; print(sys.version)"

          python -c "import sys; assert sys.version_info[:2] == (3, 12), 'Python 3.14.2 is required'"

          echo "Python 3.14.2 confirmed."

      # ---------------------------------------------------------
      # JAVA 17
      # ---------------------------------------------------------

      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      # ---------------------------------------------------------
      # SYSTEM DEPENDENCIES
      # ---------------------------------------------------------

      - name: Install system dependencies
        run: |
          set -eux

          sudo apt-get update

          sudo apt-get install -y --no-install-recommends \
            git \
            zip \
            unzip \
            autoconf \
            automake \
            autopoint \
            libtool \
            libtool-bin \
            libltdl-dev \
            pkg-config \
            cmake \
            libffi-dev \
            libssl-dev \
            zlib1g-dev \
            libncurses5-dev \
            libncursesw5-dev \
            libtinfo6 \
            ccache \
            gettext \
            curl

      # ---------------------------------------------------------
      # RUST
      # ---------------------------------------------------------

      - name: Install Rust
        run: |
          set -eux

          curl https://sh.rustup.rs -sSf | sh -s -- -y

          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"

      # ---------------------------------------------------------
      # PYTHON BUILD TOOLS
      # ---------------------------------------------------------

      - name: Install Python build tools
        run: |
          set -eux

          python -m pip install --upgrade \
            pip \
            setuptools \
            wheel \
            virtualenv

          python -m pip install \
            legacy-cgi \
            "cython>=3.1.0"

      # ---------------------------------------------------------
      # BUILDOZER
      # ---------------------------------------------------------

      - name: Install Buildozer
        run: |
          set -eux

          python -m pip install \
            git+https://github.com/kivy/buildozer.git

      - name: Verify Buildozer
        working-directory: mobile
        run: |
          set -eux

          echo "========================================"
          echo "BUILDOZER VERSION"
          echo "========================================"

          buildozer --version

      # ---------------------------------------------------------
      # VERIFY BUILDOZER CONFIGURATION
      # ---------------------------------------------------------

      - name: Verify buildozer.spec
        working-directory: mobile
        run: |
          set -eux

          echo "========================================"
          echo "BUILDOZER SPEC"
          echo "========================================"

          cat buildozer.spec

          echo "========================================"
          echo "PYTHON REQUIREMENTS"
          echo "========================================"

          grep -n "^requirements" buildozer.spec || true

          echo "========================================"
          echo "ANDROID SETTINGS"
          echo "========================================"

          grep -n "^android.api" buildozer.spec || true
          grep -n "^android.ndk" buildozer.spec || true
          grep -n "^android.minapi" buildozer.spec || true
          grep -n "^android.archs" buildozer.spec || true

          echo "========================================"
          echo "PYTHON-FOR-ANDROID SETTINGS"
          echo "========================================"

          grep -n "^p4a.bootstrap" buildozer.spec || true
          grep -n "^p4a.branch" buildozer.spec || true
          grep -n "^p4a.python_version" buildozer.spec || true

      # ---------------------------------------------------------
      # VERIFY REQUIRED PYTHON 3.14.2 SETTINGS
      # ---------------------------------------------------------

      - name: Check Python 3.14.2 Android configuration
        working-directory: mobile
        run: |
          set -eux

          echo "Checking Python 3.14.2 Android configuration..."

          grep -q "^requirements = python3==3.14.2" buildozer.spec

          grep -q "^android.api = 36" buildozer.spec

          grep -q "^android.ndk = 29" buildozer.spec

          grep -q "^android.minapi = 24" buildozer.spec

          grep -q "^android.archs = arm64-v8a,armeabi-v7a" buildozer.spec

          grep -q "^p4a.bootstrap = sdl2" buildozer.spec

          grep -q "^p4a.branch = master" buildozer.spec

          grep -q "^p4a.python_version = 3.14.2" buildozer.spec

          echo "========================================"
          echo "CONFIGURATION CHECK PASSED"
          echo "========================================"

      # ---------------------------------------------------------
      # CLEAN BUILD
      # ---------------------------------------------------------

      - name: Clean Android build
        working-directory: mobile
        run: |
          set -eux

          rm -rf .buildozer
          rm -rf bin
          rm -rf ~/.buildozer
          rm -rf ~/.cache/pip

          echo "Clean Android build directory prepared."

      # ---------------------------------------------------------
      # BUILD APK
      # ---------------------------------------------------------

      - name: Build Android APK
        working-directory: mobile
        env:
          ACCEPT_EULA: "Y"
          PYTHONFORANDROID_PREREQUISITES_INSTALL_INTERACTIVE: "0"

        run: |
          set -eux

          echo "========================================"
          echo "LOTTERYLAB ANDROID BUILD"
          echo "========================================"

          echo "Host Python:"
          python --version

          echo "Python executable:"
          python -c "import sys; print(sys.executable)"

          echo "Python version:"
          python -c "import sys; print(sys.version)"

          echo "========================================"
          echo "STARTING BUILDOZER"
          echo "========================================"

          buildozer -v --stacktrace android debug

      # ---------------------------------------------------------
      # FIND APK
      # ---------------------------------------------------------

      - name: List APK files
        if: always()
        working-directory: mobile
        run: |
          echo "========================================"
          echo "BIN DIRECTORY"
          echo "========================================"

          ls -lah bin || true

          echo "========================================"
          echo "APK FILES"
          echo "========================================"

          find . -type f -name "*.apk" -print || true

      # ---------------------------------------------------------
      # UPLOAD APK
      # ---------------------------------------------------------

      - name: Upload APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: LotteryLab-Android-APK
          path: mobile/bin/*.apk
          if-no-files-found: error
